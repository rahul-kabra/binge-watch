import { NumberUnitConverterPipe } from './number-unit-converter.pipe';

describe('NumberUnitConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberUnitConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
